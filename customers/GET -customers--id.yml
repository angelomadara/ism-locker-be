info:
  name: GET /customers/:id
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/customers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
