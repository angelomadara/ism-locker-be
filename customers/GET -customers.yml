info:
  name: GET /customers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/customers?page=1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: page
      value: "1"
      type: query
    - name: status
      value: active
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
