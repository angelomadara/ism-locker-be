info:
  name: DELETE /cards/:id
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/cards/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
