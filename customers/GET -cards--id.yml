info:
  name: GET /cards/:id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/cards/6a1ecf760473c0b47ddcd50f"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
