info:
  name: GET /cards/:id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/cards/6a2a838d35c26657ba9ebbee"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
