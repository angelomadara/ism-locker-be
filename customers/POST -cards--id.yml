info:
  name: POST /cards/:id
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/cards/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
