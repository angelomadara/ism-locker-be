info:
  name: GET /customer/card/:id
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/customers/card/2151609754"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
