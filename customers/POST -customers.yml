info:
  name: POST /customers
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/customers"
  body:
    type: json
    data: |-
      {
        "cardIds": ["1234567894"],
        "expirationDate": "2027-06-01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
