info:
  name: DELETE /customers/:id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/customers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
