info:
  name: GET /cards
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/cards?query=2184756039"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: query
      value: "2184756039"
      type: query
    - name: status
      value: disabled
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
