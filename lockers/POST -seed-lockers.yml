info:
  name: POST /seed/lockers
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/seed/lockers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
