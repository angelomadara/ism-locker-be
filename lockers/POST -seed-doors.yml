info:
  name: POST /seed/doors
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/v1/seed/doors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
