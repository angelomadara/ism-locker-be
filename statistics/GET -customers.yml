info:
  name: GET /customers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/statistics/customers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
