info:
  name: GET /transactions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/statistics/transactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
