info:
  name: POST /transaction/pin
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/pin"
  body:
    type: json
    data: |-
      {
        "pin": "1223",
        "lockerId": "6a0bf9dbca77bfe9fae6d491"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
