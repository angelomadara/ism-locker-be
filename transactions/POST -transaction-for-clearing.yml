info:
  name: POST /transaction/for-clearing
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/for-clearing"
  body:
    type: json
    data: |-
      {
        "id": "6a224ce4eabaf2b298539f8f"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
