info:
  name: POST /transaction/for-clearing
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/for-clearing"
  body:
    type: json
    data: |-
      {
        "id": "6a2a31fd2c3ec932b7abf0e3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
