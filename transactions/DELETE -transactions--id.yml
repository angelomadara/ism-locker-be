info:
  name: DELETE /transactions/:id
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/transactions/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
