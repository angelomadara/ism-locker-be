info:
  name: POST /transaction/clear/open
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/clear/open"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "lockerId": "6a0bf9dbca77bfe9fae6d48f",
        "card": "1815478677"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
