info:
  name: GET /transactions/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions/6a0d6370e874c57782c0a5a1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
