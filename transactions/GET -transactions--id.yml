info:
  name: GET /transactions/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions/6a224ce4eabaf2b298539f8f"
  params:
    - name: history
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
