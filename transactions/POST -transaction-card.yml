info:
  name: POST /transaction/card
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/card"
  body:
    type: json
    data: |-
      {
        "cardId": "9463081572",
        "lockerId": "6a26b4151862d28a21b2c950"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
