info:
  name: POST /transaction/card
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/card"
  body:
    type: json
    data: |-
      {
        "cardId": "6294703815",
        "lockerId": "6a27ff7bb313f40b2ab02910"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
