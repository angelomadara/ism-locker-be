info:
  name: GET /transactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions?status=completed"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: status
      value: for_clearing
      type: query
      disabled: true
    - name: status
      value: created
      type: query
      disabled: true
    - name: status
      value: completed
      type: query
    - name: status
      value: expired
      type: query
      disabled: true
    - name: status
      value: failed
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
