info:
  name: GET /transactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions?page=1&status=created"
  params:
    - name: page
      value: "1"
      type: query
    - name: status
      value: created
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
