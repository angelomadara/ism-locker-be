info:
  name: GET /transactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions?page=1"
  params:
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
