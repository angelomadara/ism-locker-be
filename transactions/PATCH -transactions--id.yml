info:
  name: PATCH /transactions/:id
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/transactions/6a0d2c926311dc6e9438be47"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
