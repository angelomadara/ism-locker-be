info:
  name: GET /transactions/all
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
