info:
  name: POST /transaction/expired
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transactions/expired"
  body:
    type: json
    data: |-
      {
        "id": "6a224ce4eabaf2b298539f8f"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
